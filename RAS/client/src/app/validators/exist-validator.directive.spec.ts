import { ExistValidatorDirective } from './exist-validator.directive';

describe('ExistValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ExistValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
