import { ForbiddennamesDirective } from './forbiddennames.directive';

describe('ForbiddennamesDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddennamesDirective();
    expect(directive).toBeTruthy();
  });
});
